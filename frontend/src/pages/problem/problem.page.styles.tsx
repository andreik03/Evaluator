import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
    problemContainer: {
        padding: "3vw 0 0 0",
        maxWidth: "55vw",
    },

    problemcodeEditor: {
        background: "#1c2434",
    }
}));
